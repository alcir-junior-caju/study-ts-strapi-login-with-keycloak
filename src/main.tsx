// import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  // TODO: Remove for not duplicate create user strapi
  // <React.StrictMode>
    <App />
  // </React.StrictMode>,
)
